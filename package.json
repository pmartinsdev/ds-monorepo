{
  "name": "monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "workspaces": [
    "**/packages/**/*"
  ],
  "devDependencies": {
    "turbo": "^1.9.3"
  }
}
